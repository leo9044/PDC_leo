import QtQuick 2.12
import QtQuick.Window 2.12
import QtQuick.Controls 2.12

// GearApp Main Entry Point
// This file loads the GearSelectionWidget component
GearSelectionWidget {
    id: mainWindow
    
    // The widget is already a Window, so we just instantiate it
    // All logic is in GearSelectionWidget.qml
}
